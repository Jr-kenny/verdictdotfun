# Repository Instructions

- Use `genlayer-docs` as the source of truth for GenLayer protocol, SDK, CLI, and contract behavior in this repository.
- Prefer `pnpm` for all JavaScript and TypeScript package management and script execution. Do not use `npm`.
